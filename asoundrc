pcm.!default{
    type hw;
    card PCH;
}

ctl.!default{
    type hw;
    card PCH;
}

ctl.equal {
    type equal;
}

pcm.plugequal {
    type equal;
    slave.pcm "plug:dmix";
}

pcm.!default {
    type plug;
    slave.pcm plugequal;
}

defaults.pcm.rate_converter "speexrate_medium"
