(include "./bar_modules/module.yuck")

(deflisten cpu_usage :initial 0
  `python ./scripts/cpu_usage.py`
)

(defwidget cpu []
  (module :name "cpu"
    :icon ""
    :value_widget "${cpu_usage}"
  )
)