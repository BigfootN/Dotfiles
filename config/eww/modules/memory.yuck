(include "./bar_modules/module.yuck")

(deflisten mem_usage :inital 0
  `python ./scripts/mem_usage.py`
)

(defwidget memory []
  (module :name "memory"
    :icon ""
    :value_widget "${mem_usage}"
  )
)