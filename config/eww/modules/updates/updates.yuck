(include "./modules/module.yuck")

(defpoll updates :interval "10m"
  :intial "0"
  `python ./modules/updates/updates.py`
)

(defwidget updates []
  (module :name "updates"
    :icon ""
    :value_widget "${updates}"
  )
)